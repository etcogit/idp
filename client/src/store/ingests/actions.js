/*
export const someAction = (state) => {}
*/
export const createContact = (newContact) => {
  // J'enregistre le nouveau contact dans la DB
  // this.$socket.emit('createContact', newContact)
  // J'enregistre le nouveau contact dans mon state "local"
}
